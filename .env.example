# Shikai - Environment Variables
# Copy to .env and fill in your values

# Bun registry configuration (optional - defaults to npmjs.org)
# BUN_REGISTRY=https://registry.npmjs.org
# BUN_TOKEN=your_private_registry_token

# Development
NODE_ENV=development